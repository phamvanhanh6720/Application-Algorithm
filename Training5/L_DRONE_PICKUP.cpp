#include <bits/stdc++.h>

using namespace std;

int n, k;
const int N = 3010;
int C[N], A[N];
int dp[N][N];

int main(){
    ios_base::sync_with_stdio(0); cin.tie(0);

    cin >> n >> k;
    for (int i = 1; i<=n; i++){
        cin >> C[i];
    }
    for (int i = 1; i <= n; i++){
        cin >> A[i];
    }
    memset(dp, 0, sizeof(dp));

    for (int i = 1; i<= n; i++){
        
    }
    

    return 0;
}