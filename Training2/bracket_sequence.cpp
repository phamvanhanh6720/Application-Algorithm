#include <bits/stdc++.h>

using namespace std;

int main(){
    std::string bracket_seq;
    getline(cin, bracket_seq);
    int count = 0;
    bool flag = false;

    stack<char> s;
    int length = bracket_seq.length();
    for (int i=0; i!=length, i++){
        if 
    }

    return 0;
}